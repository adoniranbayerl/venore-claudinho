// Arquivo INTERNO do fixture-plugin "a" (categoria plugin-internal): nenhum outro plugin pode
// importar isto — só o barrel index.ts. Ver cross-plugin-boundary.eslint.test.ts.
export const fixtureCrossASecret = "internal-only";
